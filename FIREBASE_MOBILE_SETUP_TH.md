# คู่มือใช้งานระบบจับฉลากฟุตบอล 7 คน

ระบบใช้ **Firebase Realtime Controller** เพื่อให้คอมพิวเตอร์หน้าควบคุม จอนำเสนอ และโทรศัพท์ทำงานร่วมกันแบบสด เหมาะสำหรับการควบคุมจากคนละเครื่องหรือคนละเครือข่ายอินเทอร์เน็ต

## ลิงก์ใช้งาน

- หน้าควบคุม: `https://checkfile2568-ops.github.io/rapee69/control.html`
- หน้าจอนำเสนอ: `https://checkfile2568-ops.github.io/rapee69/display.html`
- หน้าควบคุมมือถือ: สแกน QR จากหน้าควบคุม หรือกดปุ่มเปิดหน้าควบคุมมือถือ

## ก่อนเริ่มงาน

1. เปิด **หน้าควบคุม** บนคอมพิวเตอร์เจ้าหน้าที่ แล้วกด `Ctrl + F5` หนึ่งครั้ง
2. กด **เปิดหน้าจอนำเสนอ** และแชร์เฉพาะแท็บนี้ใน Google Meet
3. ตรวจแถบสถานะให้ขึ้น **Firebase Realtime API SDK Online** ทุกหน้าที่ใช้งาน
4. กด **เริ่ม / แสดง QR ควบคุมมือถือ** แล้วสแกน QR ด้วยโทรศัพท์เจ้าหน้าที่
5. บนโทรศัพท์ต้องเห็นสถานะ **ซิงก์มือถือพร้อม** ก่อนเริ่มจับฉลาก
6. หน้าควบคุมมีรายการตรวจความพร้อม: โหมดถ่ายทอดสด, ไม่มีผลซ้อมค้าง, ไม่ได้ล็อกผล และพบจอนำเสนอ

## วิธีจับฉลาก

1. เลือกตำแหน่งจาก **โถที่ 1 — ตำแหน่งการแข่งขัน** เช่น A1 หรือ B4
2. เลือกทีมจาก **โถที่ 2 — หมายเลขทีม**
3. ตรวจผลที่แสดง แล้วกด **ยืนยันผล** เพียงครั้งเดียว
4. หน้าจอนำเสนอจะแสดงช่วง “กำลังเปิดฉลาก…” ก่อนเผยผล และไฮไลต์ช่องล่าสุด
5. ทำซ้ำจนครบ 7 ทีม จากนั้นตรวจความถูกต้องและกด **ล็อกผล**

บนมือถือ ปุ่มยืนยันผลไม่มีหน้าต่างถามซ้ำ เพื่อให้ใช้งานในพิธีได้รวดเร็ว จึงควรตรวจตำแหน่งและทีมบนจอก่อนกดทุกครั้ง

## หากมือถือหรือจอนำเสนอไม่ตามข้อมูล

1. ดูแถบสถานะ Firebase บนหน้าอุปกรณ์นั้นก่อน ต้องเป็น **Online**
2. ตรวจว่าอุปกรณ์เชื่อมอินเทอร์เน็ตอยู่ แล้วรอไม่เกิน 2–3 วินาที
3. เปิด QR ใหม่จากหน้าควบคุม แล้วสแกนใหม่หากโทรศัพท์เปิดทิ้งไว้นาน
4. รีเฟรชเฉพาะหน้าที่มีปัญหาด้วย `Ctrl + F5` หรือปิดแล้วเปิดลิงก์ QR ใหม่
5. หากอินเทอร์เน็ตขัดข้อง ให้ใช้หน้าควบคุมและหน้าจอนำเสนอในคอมเครื่องเดียวกันชั่วคราว ระบบยังสื่อสารกันผ่านเบราว์เซอร์ในเครื่องได้

## การตั้งค่า Firebase สำหรับผู้ดูแลระบบ

ไฟล์ `firebase-config.js` ต้องมี `apiKey`, `projectId` และ `databaseURL` ของโครงการ Firebase และทุกอุปกรณ์ต้องใช้ `DRAW_FIREBASE_PATH` เดียวกัน เช่น

```js
window.DRAW_FIREBASE_PATH = "rapee69/live-2569-07-31";
```

ค่า Firebase Web Config ไม่ใช่รหัสผ่าน แต่ **Firebase Rules** เป็นส่วนคุ้มครองข้อมูล หากเปิด Rules สำหรับงานนี้ชั่วคราว ให้จำกัดเฉพาะ path ของรอบงาน และหลังจบงานต้องปิดสิทธิ์อ่าน/เขียน หรือลบข้อมูลของรอบงานออกจาก Realtime Database

## หลังจบพิธี

1. ตรวจผลในหน้า **ผลอย่างเป็นทางการ**
2. ใช้ปุ่ม **บันทึกผลเป็น PNG** เพื่อส่งเข้ากลุ่มงาน
3. ล็อกผลไว้ก่อนปิดระบบ
4. ปิดสิทธิ์ Firebase Rules ของ path รอบงาน และลบข้อมูลเมื่อตามระเบียบงาน
