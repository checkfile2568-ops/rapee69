# บันทึกแนวทางการปรับรูปแบบสำหรับรายการอื่น

เอกสารนี้เป็นแนวทางสำหรับต่อยอดระบบจับฉลากวันรพี 69 ไปใช้กับกิจกรรมอื่น โดยยังคงระบบเดิมให้ใช้งานได้ตามปกติ

## สถานะของระบบปัจจุบัน

เวอร์ชัน 1.6 สร้างมาเพื่อการแข่งขันฟุตบอล 7 ทีมโดยเฉพาะ จึงกำหนดรายชื่อทีม ตำแหน่ง `A1–A3` และ `B1–B4` หน้าสรุปสาย A สาย B ผัง Play-off และตารางแข่งขันไว้ในระบบแล้ว

ส่วนที่ใช้ซ้ำได้ทันทีคือหน้าควบคุม หน้าจอนำเสนอ การยืนยันผล การล้างคู่ปัจจุบัน การย้อนกลับผลล่าสุด การล็อกผล หน้าควบคุมมือถือ QR สำหรับเชื่อมมือถือ และการซิงก์แบบ Google Apps Script

## หลักการปรับในอนาคต

ไม่ควรรื้อหรือแก้ไฟล์หลักของเวอร์ชันที่ใช้กับงานจริงทุกครั้ง แนะนำให้สร้างระบบรุ่นถัดไปแบบ **ตั้งค่ารายการ** แยกจากโปรแกรมหลัก โดยเก็บข้อมูลของแต่ละงานไว้ในไฟล์ตั้งค่า เช่น `events/football-rapee69.json`

ข้อมูลตั้งค่าของแต่ละรายการควรมีอย่างน้อยดังนี้

```text
รหัสรายการ / ชื่องาน / วันที่ / หน่วยงานเจ้าภาพ
รายชื่อผู้เข้าร่วม
โถจับฉลากและตัวเลือกในแต่ละโถ
ตำแหน่งหรือช่องรับผล
รูปแบบหน้าจอนำเสนอ
รูปแบบสรุปผลและตารางต่อเนื่อง (ถ้ามี)
```

ระบบหลักจะอ่านไฟล์ตั้งค่าแล้วสร้างหน้าควบคุม หน้ามือถือ และหน้านำเสนอให้ตรงกับรายการนั้น โดยไม่แก้กลไก QR หรือการยืนยันผล

## รูปแบบรายการที่รองรับได้

1. **แบ่งกลุ่ม / แบ่งสาย** — จับชื่อผู้เข้าร่วมเข้าช่อง A1, A2, B1 เป็นต้น เหมือนระบบฟุตบอลปัจจุบัน
2. **กำหนดลำดับ** — จับหมายเลขลำดับ เช่น ลำดับเดินขบวน ลำดับนำเสนอ หรือคิวแข่งขัน
3. **จับรางวัล** — จับผู้ได้รับรางวัล โดยตัดชื่อที่ได้รับแล้วออกอัตโนมัติ
4. **การแข่งขันแบบกำหนดผังเอง** — กำหนดช่องในสายแข่งขันหรือ Bracket ได้ตามผังของงาน

หากเป็นกติกาพิเศษ เช่น ทีมวาง ห้ามหน่วยงานเดียวกันอยู่กลุ่มเดียวกัน หรือจำนวนผู้เข้าร่วมไม่แน่นอน ระบบควรเพิ่ม “กฎของรายการ” เฉพาะส่วนนั้น ไม่ควรฝังไว้ในหน้าจอหลัก

## ขั้นตอนใช้งานที่ควรเป็นในรุ่นต่อไป

1. ผู้จัดเลือกแม่แบบรายการ เช่น แบ่งสาย / จับรางวัล / จัดลำดับ
2. กรอกหรือวางรายชื่อและจำนวนช่องรับผล
3. ระบบตรวจว่าจำนวนรายการและจำนวนช่องตรงกัน พร้อมแสดงตัวอย่างก่อนเริ่ม
4. ล็อกการตั้งค่า แล้วเปิด QR ให้มือถือควบคุม
5. ดำเนินการจับฉลากและใช้หน้าสรุปที่สร้างจากข้อมูลของรายการนั้น

กรณีรูปแบบตรงกับแม่แบบ จะเปลี่ยนเพียงข้อมูล ไม่ต้องแก้โค้ด ส่วนรายการที่มีผังหรือกติกาใหม่จริง ๆ จึงค่อยเพิ่มแม่แบบใหม่หนึ่งครั้ง

## ข้อควรคุมข้อมูลเมื่อรองรับหลายรายการ

- เก็บข้อมูลแต่ละงานแยกด้วย `eventId` เพื่อไม่ให้ผลจากงานหนึ่งปะปนกับอีกงาน
- QR ของงานจริงควรผูกทั้ง `eventId` และรหัสรอบควบคุม
- คงระบบสำรองจาก `control.html` บนคอมไว้เสมอ แม้ใช้มือถือควบคุม
- ก่อนเริ่มงานให้ตรวจชื่อ จำนวนรายการ และหน้าสรุปจากโหมดซ้อม แล้วล็อกการตั้งค่าก่อนเปิด QR

## ข้อเสนอสำหรับการพัฒนาครั้งถัดไป

ให้เริ่มจากการย้ายข้อมูลฟุตบอลวันรพี 69 ออกมาเป็น “ตัวอย่างรายการแรก” โดยให้ผลลัพธ์หน้าจอเหมือนเดิมทุกประการ แล้วเพิ่มหน้าเลือกแม่แบบและหน้ากรอกข้อมูลอย่างง่าย วิธีนี้ปลอดภัยที่สุด เพราะระบบที่ใช้อยู่ยังใช้ได้ต่อเนื่อง และรายการอื่นจะไม่กระทบงานฟุตบอลเดิม
