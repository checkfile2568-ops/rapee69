# ตั้งค่า QR ควบคุมมือถือด้วย Google Apps Script

ตั้งค่าครั้งเดียวก่อนวันงาน หลังจากนั้นเปิดหน้าควบคุมและสแกน QR ได้เลย แม้คอมกับมือถือใช้คนละเครือข่าย เช่น Wi-Fi สำนักงานกับ 4G/5G

## 1. สร้าง Apps Script Web App

1. เข้า [Google Apps Script](https://script.google.com/) แล้วสร้าง Project ใหม่
2. ลบโค้ดเดิมทั้งหมด แล้ววางเนื้อหาจากไฟล์ `GAS_BACKEND.gs` ของเวอร์ชัน 1.6
3. กด **Deploy → New deployment → Web app**
4. ตั้งให้ Script ทำงานภายใต้บัญชีผู้เผยแพร่ และตั้งการเข้าถึงให้มือถือกับคอมเปิด URL ได้
5. กด Deploy และคัดลอก Web app URL ที่ลงท้ายด้วย `/exec`

## 2. ใส่ URL เพียงครั้งเดียว

เปิด `sync-config.js` แล้วใส่ URL ในเครื่องหมายคำพูด:

```js
window.DRAW_REMOTE_SYNC_URL = "https://script.google.com/macros/s/DEPLOYMENT_ID/exec";
```

อัปโหลด `sync-config.js` พร้อมไฟล์เวอร์ชัน 1.6 ขึ้น GitHub Pages แล้วกด `Ctrl + F5` บนคอมเพื่อโหลดไฟล์ใหม่

## 3. ใช้งานวันจริง

1. เปิด `control.html` บนคอม
2. กด **เริ่ม / แสดง QR ควบคุมมือถือ**
3. ระบบสร้างรหัสรอบและ QR ให้เอง พร้อมจำไว้ในคอมเครื่องนั้น
4. เปิด `display.html` ด้วยปุ่ม **เปิดหน้าจอนำเสนอ** จากหน้าควบคุม แล้วแชร์แท็บนี้ใน Google Meet
5. ใช้มือถือสแกน QR และควบคุมได้ทันที

## กรณีมือถือจอดับหรืออินเทอร์เน็ตหลุด

- ไม่สร้าง QR ใหม่อัตโนมัติ
- ปลุกหน้าจอและกลับมาที่แท็บเดิม: มือถือจะส่งสัญญาณเชื่อมต่อใหม่เอง
- หากเบราว์เซอร์ถูกปิด: สแกน QR เดิมบนหน้าควบคุมอีกครั้ง
- กด **สร้าง QR รอบใหม่** เฉพาะกรณีต้องการตัดการใช้งานจากมือถือเครื่องเดิมจริง ๆ

## ความปลอดภัย

QR มีรหัสรอบแบบสุ่มและไม่เก็บชื่อผู้ใช้หรือข้อมูลส่วนตัว แต่ยังไม่ใช่ระบบบัญชีผู้ใช้หรือการเข้ารหัสสิทธิ์ระดับองค์กร ดังนั้นควรแสดง QR ให้เฉพาะเจ้าหน้าที่ควบคุม และหลังจบงานควรลบหรือปิดการเผยแพร่ Apps Script หากไม่ใช้ต่อ
